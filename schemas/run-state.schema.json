{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://skillweave.dev/schemas/run-state/v1",
  "title": "Run State Schema",
  "description": "SkillWeave Runtime Run State — versioned, persisted processing unit",
  "type": "object",
  "required": ["run_id", "root_run_id", "state", "version", "created_at", "role"],
  "properties": {
    "run_id": {
      "type": "string",
      "description": "Unique identifier for this process instance"
    },
    "root_run_id": {
      "type": "string",
      "description": "Root run identifier for system-of-systems traceability"
    },
    "parent_run_id": {
      "type": ["string", "null"],
      "description": "Immediate parent run identifier"
    },
    "state": {
      "type": "string",
      "description": "Current state from the canonical lowercase status vocabulary. The enum mirrors RunStateModel exactly.",
      "enum": [
        "preflight",
        "batch_selection",
        "lane_plan",
        "implement",
        "verify",
        "review_gate",
        "fix_retry",
        "integrate",
        "advance_or_stop",
        "sandbox_preflight",
        "in_progress",
        "preflight_complete",
        "blocked_waiting_for_gate",
        "review_required",
        "failed"
      ]
    },
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "Optimistic concurrency version"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "ended_at": {
      "type": ["string", "null"],
      "format": "date-time"
    },
    "role": {
      "type": "string",
      "description": "Accountable role for this process instance"
    },
    "metadata": {
      "type": "object",
      "description": "Extensible metadata"
    }
  },
  "additionalProperties": false
}
