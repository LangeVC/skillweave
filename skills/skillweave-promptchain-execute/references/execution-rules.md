# Execution Rules

- run steps in sequence by default
- do not skip validation if intermediate validation is required
- do not claim completion without required evidence
- follow failure handling if a blocker appears
