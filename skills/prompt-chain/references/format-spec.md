# Format Spec

The prompt-chain skill expects a sequence with the required sections listed in the main documentation.
The sequence author should keep the structure explicit and avoid hidden dependencies.
